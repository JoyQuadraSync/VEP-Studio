import { SnapshotCompleteness, SnapshotFreshness } from '../contracts/tiktok-account-snapshot-provenance';
export type TikTokMetricUnavailableReason = 'missing_view_count' | 'missing_engagement_counter' | 'zero_view_count' | 'invalid_counter' | 'unsafe_aggregate' | 'missing_previous_measurement' | 'non_increasing_measurement_time' | 'counter_regression' | 'partial_snapshot' | 'stale_snapshot' | 'unknown_freshness';
export interface TikTokVideoAnalytics { readonly videoId: string; readonly measuredAt: string; readonly viewCount?: number; readonly engagementCount?: number; readonly engagementRate?: number; readonly viewsPerDay?: number; readonly unavailableReasons: readonly TikTokMetricUnavailableReason[]; }
export interface TikTokAccountAnalyticsSummary { readonly summaryId: string; readonly connectionId: string; readonly generatedAt: string; readonly observationWindow: Readonly<{ from: string; to: string }>; readonly freshness: SnapshotFreshness; readonly completeness: SnapshotCompleteness; readonly sampleSize: number; readonly videos: readonly TikTokVideoAnalytics[]; readonly topVideosByViews: readonly TikTokVideoAnalytics[]; }
