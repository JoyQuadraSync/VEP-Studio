export enum WorkflowState {
  CREATED = 'created',
  RUNNING = 'running',
  COMPLETED = 'completed',
  FAILED = 'failed'
}

export enum WorkflowStepResultStatus {
  COMPLETED = 'completed',
  FAILED = 'failed'
}
