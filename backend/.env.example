# This file is an example only. VEP Studio does not load .env files automatically,
# and dotenv is intentionally not installed. Export these values in the shell or
# provide them through another process launcher before using a billable entry point.
OPENAI_API_KEY=
OPENAI_MODEL=
# The live test also requires this explicit opt-in. Generated German content and
# marketing claims require manual semantic review before use.
OPENAI_LIVE_TEST=false
